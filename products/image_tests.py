"""
Image tests removed.

The project's image upload tests were removed per request to drop
image-related functionality and dependencies (Pillow). If you need
to reintroduce image tests later, restore them from version control
or re-add test code here.
"""
